# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint nyc coverage output
coverage
test-payload.json
.yarn
.vscode
.pnp.cjs
.pnp.loader.mjs
# don't prettify ea-generator folder since it contains invalid syntax
scripts/generator-adapter

packages/k6/k8s/templates
